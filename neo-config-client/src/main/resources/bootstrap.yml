
spring:
  cloud:
    config:
      name: neo-config
      profile: dev
      label: master
      #uri: http://localhost:8001/
      discovery:
        enabled: true
        serviceId: config-server
        
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/